apiVersion: v1
kind: Service
metadata:
  name: {{ include "hdmi-matrix-proxy.fullname" . }}
  labels:
    {{- include "hdmi-matrix-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hdmi-matrix-proxy.selectorLabels" . | nindent 4 }}
